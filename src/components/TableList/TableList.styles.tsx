import { styled, Typography } from '@mui/material';

export const TableLabel = styled(Typography)`
    font-size: 14px;
    color: #bababa;
`;

export const TableCell = styled(Typography)`
    font-size: 14px;
    color: #000000;
`;

export const TableStatus = styled(Typography)`
    position: absolute;
    inset: 0;
    top: 19.25px;
    font-size: 12px;
    font-weight: 700;
`;
